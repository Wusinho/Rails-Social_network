
<p>
  <strong>Name:</strong>
  <%= @category.name %>
</p>
<div class="row">
  <% @recent_related_articles.each do |article| %>

        <div class="col-3 p-0">
          <%= link_to article.title, article_path(article.id) %>
            <%= image_tag article.image_url  %>
            
        </div>
  <% end %>
</div>


<%= link_to 'Edit', edit_category_path(@category) %> |
<%= link_to 'Back', categories_path %>
