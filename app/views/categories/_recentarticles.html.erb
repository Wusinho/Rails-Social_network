    <div class='col-3 py-3 d-flex flex-column justify-content-between text-white' style="background: url(<%= category.articles.ordered_by_most_recent.first.image_url  %>); background-size : cover " >
        <%= link_to category.name, category_path(category.id), class:'text-warning' %>
        <%= category.articles.ordered_by_most_recent.first.title %>
    </div>